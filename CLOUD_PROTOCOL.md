# ATOA Delegation & Runnable Demo Protocol v1

ATOA 让用户级 Codex 处理云端项目任务，而无需 clone 完整仓库、配置项目运行环境或手工部署。
用户继续使用自己的私有 Coding Agent，自主选择模型并管理 Token，不需要购买平台提供的模型 Token。客户端 Agent 发起修改请求；云端统一 Server 负责合同预检、最小 Context、项目 Skills、权限、验证、合并、部署和最终版本。

## 核心对象

ATOA 的用户可见版本不是孤立的代码提交，而是一个通过服务端验收、可以直接打开的只读 Demo：

1. 客户端 Agent 发送用户本次修改目标和明确验收标准。
2. 服务端控制平面在创建任务前按确定性规则检查项目能力与安全策略。
3. 服务端返回绑定 `base_revision`、写权限和文件 SHA-256 的最小 Context manifest。
4. 客户端 Agent 在授权范围内完成实现；缺失信息通过增量 Context 请求补充。
5. 客户端返回紧凑候选操作和证据，不能直接写公开项目。
6. 服务端独立扫描、运行固定测试并原子合并。
7. 接受的贡献生成 contribution 记录、最终 revision 和不可变的可运行 Demo 快照。

失败候选只产生 `revision_requested`，不会改变公开项目，也不会生成 Demo 版本。

账户注册与登录是两个独立操作：部署方邀请码只允许注册新账户；登录必须匹配已注册邮箱和
salted scrypt 密码，客户端不能通过提交任意邮箱创建身份。已登录用户可以创建持久化托管项目，
浏览器使用 Secure HttpOnly Session Cookie，不把 Token 写入浏览器存储。内置项目可以声明为
所有注册用户可参与；托管项目默认只有创建者和创建者明确添加的成员可见。项目目录、文件、
Context、任务、贡献、预览和 Demo 使用同一套项目 ACL。只有创建者可以管理成员和只读项目
Skill。服务端按照项目清单运行预先声明的固定测试；客户端不能指定任意测试命令。

当前版本使用源码 Context 路径做并发占用：Context 不重叠的任务可同步派发，重叠任务继续排队；增量 Context 也不能跨入其他活动任务的占用范围。若全局 revision 仅由不相关文件推进，候选目标文件仍匹配任务 SHA-256，服务端会在当前项目状态上重新验证并原子合并。目标文件变化时仍返回 revision conflict。更细粒度的符号与显式依赖分析仍在开发中。服务端始终是确定性的控制平面，不承担 Agent 推理或代码实现。

## 本地数据边界

ATOA 只处理用户明确发起的当前共创任务。协议允许客户端发送：

- 当前任务的修改请求和验收标准；
- 当前任务的进度与具体的增量 Context 请求；
- 当前任务的候选操作、候选文件和执行证据。

协议不会要求客户端读取或上传：

- 其他 Codex 对话 session 或无关对话内容；
- 任务范围外的本地文件、其他项目、个人文档；
- 环境变量、浏览器数据、API Key、Token 或其他本地凭证；
- 完整本地仓库或客户端可编辑项目副本。

这里的“当前 session 相关内容”仅指完成这一次用户授权任务所必需、并被明确放入工具调用参数的内容，
不代表上传整个 Codex 会话历史。Context 内容来自项目服务端，客户端本地缓存是按服务端、身份和项目隔离的只读内容寻址缓存。

原始 Prompt、验收标准和任务对话只对授权参与者可见。项目成员可以查看脱敏后的开发意图、实现摘要、变更文件、服务端验证结果和可运行 Demo；摘要会隐藏凭证、联系方式、本地路径和外部链接。

## 私有 AI 能力保护

用户可能在本地开发依赖私有 API 的 AI 功能。候选进入项目之前，服务端会比较正式基线并阻断新增的：

- 常见 API Key、访问 Token、云凭证、密码或密钥赋值；
- `document.cookie`、`sessionStorage` 等敏感浏览器数据访问；
- `fetch`、WebSocket、SSE 等主动网络外发；
- 外部脚本、`eval`、`new Function` 等危险能力。

项目命名空间的 `localStorage` 可用于非敏感应用状态。真正需要第三方 API 的项目应由平台管理员先配置受控的服务端能力，
而不是让客户端把个人密钥写入公共候选。

## 一致性与重试

- 每个候选绑定 `base_revision` 和每个目标文件的 `expected_hash`。
- 优先使用 `insert_before`、`insert_after`、`replace` 等紧凑操作。
- 锚点缺失、歧义或哈希过期时，整个候选原子失败。
- 候选代码变化时上传新候选；只修复服务端策略或验证环境时用 `reuse_candidate: true` 复验私有缓存候选。
- 全局 revision 推进后，仅目标文件哈希未变化的并行候选可以继续；旧任务不能覆盖已变化的目标文件。

`atoa-cloud/v1` 工作区流程仅用于兼容；默认流程是无需 clone、无需客户端项目副本的 `atoa-delegation/v1`。
